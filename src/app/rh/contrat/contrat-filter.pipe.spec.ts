import { ContratFilterPipe } from './contrat-filter.pipe';

describe('ContratFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContratFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
