import { Component } from '@angular/core';

@Component({
  selector: 'app-permissions-home',
  template: `
    <div class="card border-0 shadow-sm p-4">
      <h3 class="mb-2">Permissions</h3>
      <p class="text-muted">Page d’accueil du module Permissions (placeholder).</p>
      <!-- Ajoute ici plus tard: liste des permissions, création, etc. -->
    </div>
  `
})
export class PermissionsHomeComponent {}
